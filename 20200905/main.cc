#include <string>
#include <iostream>
#include <vector>
using std::vector;
using namespace std;

class Solution {
  public:             // Access specifier
    vector<vector<int>> threeSum(vector<int>& nums) {
      vector<vector<int>> res;
      return res;
    }
};

int main() {
  Solution solution;  // Create an object of MyClass
  cout << solution.threeSum() << "\n";
  return 0;
}
